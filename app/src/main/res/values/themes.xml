<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.NFCTag" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.NFCTag" parent="Base.Theme.NFCTag" />

    <style name="Theme.NFCTag.Fullscreen" parent="Theme.NFCTag">
        <item name="android:actionBarStyle">@style/Widget.Theme.NFCTag.ActionBar.Fullscreen</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
    </style>

    <style name="ThemeOverlay.NFCTag.FullscreenContainer" parent="">
        <item name="fullscreenBackgroundColor">@color/light_blue_600</item>
        <item name="fullscreenTextColor">@color/light_blue_A200</item>
    </style>
</resources>